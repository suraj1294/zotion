const config = {
  providers: [
    {
      domain: "https://clerk.suraj-patil.in", //"https://complete-basilisk-61.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default config;
